{{- if .Values.customContainerImage }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.applicationName }}-pull-secret
  namespace: {{ .Values.namespace }}
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: {{ .Values.dockerconfigjson }}
{{- end }}
